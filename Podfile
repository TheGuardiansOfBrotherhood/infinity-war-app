# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'InfinityWarApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase/Core'

  # Pods for InfinityWarApp

  target 'InfinityWarAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InfinityWarAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
